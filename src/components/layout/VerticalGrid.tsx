/**
 * Renders the vertical grid lines. Use inside sections that have a dark bg layer
 * so the grid appears in front of the background and below the section content.
 */
export function VerticalGrid() {
  return <div className="vertical-grid-in-section" aria-hidden />
}
